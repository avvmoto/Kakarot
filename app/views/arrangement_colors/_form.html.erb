<%= form_for(@arrangement_color) do |f| %>
  <% if @arrangement_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arrangement_color.errors.count, "error") %> prohibited this arrangement_color from being saved:</h2>

      <ul>
      <% @arrangement_color.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :arrangement_id %><br>
    <%= f.number_field :arrangement_id %>
  </div>
  <div class="field">
    <%= f.label :color_id %><br>
    <%= f.number_field :color_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
