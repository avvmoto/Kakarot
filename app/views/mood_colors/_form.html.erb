<%= form_for(@mood_color) do |f| %>
  <% if @mood_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mood_color.errors.count, "error") %> prohibited this mood_color from being saved:</h2>

      <ul>
      <% @mood_color.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mood_id %><br>
    <%= f.number_field :mood_id %>
  </div>
  <div class="field">
    <%= f.label :color_id %><br>
    <%= f.number_field :color_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
