<%= form_for(@recipe_material) do |f| %>
  <% if @recipe_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe_material.errors.count, "error") %> prohibited this recipe_material from being saved:</h2>

      <ul>
      <% @recipe_material.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recipe_id %><br>
    <%= f.number_field :recipe_id %>
  </div>
  <div class="field">
    <%= f.label :material_id %><br>
    <%= f.number_field :material_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
