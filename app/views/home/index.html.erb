<h3><%= link_to '配色', root_path, class: "vertical-text" %></h3>
<h3><%= link_to '配食', @recipe, class: "vertical-text" %></h3>

<div id='tweets' class='time1'>
  <% @tweets.each do |tweets| %>
  <div class='tweet vertical-text'><%= tweets.massage %></div>
  <% end %>
</div><!-- /#twiiets -->

<div id=color_name' class='time1'>
  <% @colors.zip(%w(l c r)).each do |color, position| %>
  <div class='layout_<%= position %> color_<%= format_code(color) %>'></div>
  <% end %>
</div><!-- /#color_name -->

<div id=color_name' class='time1'>
  <% @colors.zip(%w(t m b)).each do |color, position| %>
  <div class='layout_<%= position %> color_<%= format_code(color) %> per_<%= format_weight(color)  %>'></div>
  <% end %>
</div><!-- /#color_layout -->
